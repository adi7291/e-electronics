{
  "name": "server",
  "version": "1.0.0",
  "description:": "back-end project with express and typescript ",
  "main": "server.js",
  "scripts": {
    "start": "tsc && node dist/server.js",
    "dev": "nodemon src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anil",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.1",
    "nodemon": "^3.1.10",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
